/**
 * @type {import("tailwindcss/tailwind-config").TailwindConfig}
 */
const config = {
  purge: ['./dist/*.html', './dist/*.js'],
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
};

module.exports = config;
